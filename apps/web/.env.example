# Server-only URL used by Next.js Server Components and Server Actions.
KNOWLEDGE_API_BASE_URL=http://localhost:8080

# Browser-reachable backend origin used only for the focused OAuth login redirect.
# This is server configuration; do not rename it with a NEXT_PUBLIC_ prefix.
KNOWLEDGE_API_BROWSER_BASE_URL=http://localhost:8080
